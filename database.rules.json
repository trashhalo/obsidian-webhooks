{
  "rules": {
    "users": {
      "$uid": {
      	".read": "$uid === auth.uid && auth.provider != 'anonymous'"
      }
    },
    "buffer": {
      "$uid": {
        ".read": "$uid === auth.uid && auth.provider != 'anonymous'"
      }
    },
  }
}